---
- name: test env var
  hosts: localhost
  gather_facts: no

  tasks:
    - name: show env var
      debug:
        msg: "This is from env vars {{ lookup('env', 'HASHI_VAULT_ENV') }}"

    - name: show env var
      debug:
        msg: "This is from extra vars {{ HASHI_VAULT_EXTRAVAR }}"